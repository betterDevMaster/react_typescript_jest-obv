// Only thing these methods do is fire off a request to the backend to record
// some telemetry. Just stubs for testing...

export const socketConnected = jest.fn(() => {})

export const socketDisconnected = jest.fn(() => {})
